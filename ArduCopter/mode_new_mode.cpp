#include "Copter.h"

/*
 * Init and run calls for new flight mode
 */

// new_mode_init - initialise new mode controller
bool ModeNewMode::init(bool ignore_checks)
{

    return true;
}

// new_mode_run - runs the new mode controller
// should be called at 100hz or more
void ModeNewMode::run()
{
    
}